export const getLinariaMetadata = value => {
  if (typeof value === 'object' && value !== null) {
    const metadata = value.linaria;
    if (typeof metadata === 'object' && metadata !== null) {
      // eslint-disable-next-line no-param-reassign
      delete value.linaria;
      return metadata;
    }
  }
  return undefined;
};
const withLinariaMetadata = value => typeof value === 'object' && value !== null && typeof value.linaria === 'object';
export default withLinariaMetadata;
//# sourceMappingURL=withLinariaMetadata.js.map